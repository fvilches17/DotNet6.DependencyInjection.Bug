@page "/"
@page "/animal-sounds"
@using Injector.Models

@foreach (var animal in Animals)
{
    <p>@animal.GetType().Name: @animal.MakeSound()</p>
}

@code {
    [Inject]
    protected IEnumerable<IAnimal> Animals { get; set; }
}